import { CloseButtonProps } from "../close-button";
export declare type DrawerCloseButtonProps = CloseButtonProps;
/**
 * `DrawerCloseButton` is used closes the drawer dialog.
 *
 * You don't need to pass the `onClick` to it, it gets the
 * `close` action from the drawer context.
 */
export declare const DrawerCloseButton: any;
